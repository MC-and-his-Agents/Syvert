#!/usr/bin/env bash

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"

cd "${REPO_ROOT}"
export PYTHONPATH="${REPO_ROOT}${PYTHONPATH:+:${PYTHONPATH}}"
python3 scripts/docs_guard.py --mode pre-commit
python3 scripts/spec_guard.py --mode pre-commit --staged-only
